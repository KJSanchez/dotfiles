# -*- mode: snippet -*-
# name: bpf
# uuid: bpf
# key: bpf
# condition: t
# --

profile = BaseProfileFactory()
success = self.client.login(email=profile.user.email, password="password")
self.assertTrue(success)
response = self.client.get("$1")
self.assertEqual(response.status_code, 200)